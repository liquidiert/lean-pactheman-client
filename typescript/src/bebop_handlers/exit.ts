import { IExitMsg } from '../models/pactheman.models';
import BebopHandler from './bebopHandler';

export default class ExitMsgHandler implements BebopHandler {
    handleMessage(sender: any, message: any): void {
        // TODO: implement handle message
    }
}